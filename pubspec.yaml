name: CityOne
description: "A new CityOne project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.11.4


dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  google_maps_flutter: ^2.5.0
  geolocator: ^10.0.0
  flutter_map_tile_caching: ^9.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.6
  firebase_messaging: ^15.2.10
  http: 1.6.0
  flutter_local_notifications: ^17.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  provider: ^6.0.5
  shared_preferences: ^2.2.2
  flutter_lints: ^6.0.0


flutter:

  
  uses-material-design: true

  